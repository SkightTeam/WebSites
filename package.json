{
  "name": "skight-itech-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Professional portal site for Isaac Wang / Skight I-Tech Inc.",
  "scripts": {
    "build": "node scripts/build-static.js",
    "serve": "npx http-server dist -p 4000",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "http-server": "^14.1.1"
  }
}
